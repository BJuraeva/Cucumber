package utils;

import org.openqa.selenium.WebDriver;

public class CommonMethods {
    public static WebDriver driver; // will work when added static key word otherwise we should create the obj of the class
}
